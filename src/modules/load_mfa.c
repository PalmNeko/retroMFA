
#include <stdlib.h>
#include "image.h"

/**
 * NULL pointer 区切りのリスト
 */
image_lst_t *load_mfa(int fd)
{
    image_lst_t *images;
    (void)fd;

    images = NULL;
    // ロードする。
    return (images);
}
